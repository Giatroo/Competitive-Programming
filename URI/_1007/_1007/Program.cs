using static System.Console;

namespace _1007
{
    class Program
    {
        static void Main(string[] args)
        {
            int n1 = (int.Parse(ReadLine()) * int.Parse(ReadLine()));
            int n2 = (int.Parse(ReadLine()) * int.Parse(ReadLine()));
            WriteLine("DIFERENCA = "+(n1-n2));
        }
    }
}
